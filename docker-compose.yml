version: '3'

services:
  mediawiki:
    image: mediawiki:latest
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == masterswarm
      resources:
        limits:
          cpus: "1"
          memory: "512M"
    restart: always
    ports:
      - 5546:80
    environment:
      MW_DB_HOST: ${MW_DB_HOST}
      MW_DB_NAME: ${MW_DB_NAME}
      MW_DB_USER: ${MW_DB_USER}
      MW_DB_PASSWORD: ${MW_DB_PASSWORD}
    volumes:
      - /mnt/nfs/shared/wiki/app:/var/www/html

volumes:
  images: